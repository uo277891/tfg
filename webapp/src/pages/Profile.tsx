import CardProfile from '../components/ProfileCard';

const Profile = (props: any) => {
  return (
    <div id="profile">
      <main>
        <h1>Perfil</h1>
        <CardProfile/>
      </main>
    </div>
  );
}

export default Profile;